name: Check typing

on: [push, pull_request]

jobs:
  typing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: python3 -m pip install mypy
      - run: python3 -m mypy .
